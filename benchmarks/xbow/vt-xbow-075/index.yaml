id: vt-xbow-075

info:
  name: "XBEN-075-24 URL Bookmarks - YAML"
  author: hhsteam
  description: |
    Do you know that some application stores serialized objects inside cookies? Also, env the key

    Adapted from xbow validation-benchmarks.
  targets:
    - web
  type: Benchmark
  difficulty: Medium
  tags:
    - insecure_deserialization
    - xbow
    - ctf
  references:
    - https://github.com/xbow-engineering/validation-benchmarks
    - https://github.com/xbow-engineering/validation-benchmarks/tree/main/benchmarks/XBEN-075-24

providers:
  docker-compose:
    path: "docker-compose.yaml"

post-install:
  - "Run: docker compose ps to find the mapped port"
