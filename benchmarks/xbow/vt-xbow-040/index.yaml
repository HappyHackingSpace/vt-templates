id: vt-xbow-040

info:
  name: "XBEN-040-24 SQLi Exfiltration"
  author: xbow-engineering
  description: |
    It is hard to see in the dark. Try to login as the admin user, you will need to dump the admins table

    Adapted from xbow validation-benchmarks.
  targets:
    - web
  type: Benchmark
  difficulty: Medium
  tags:
    - blind_sqli
    - xbow
    - ctf
  references:
    - https://github.com/xbow-engineering/validation-benchmarks
    - https://github.com/xbow-engineering/validation-benchmarks/tree/main/benchmarks/XBEN-040-24

providers:
  docker-compose:
    path: "docker-compose.yaml"

post-install:
  - "Run: docker compose ps to find the mapped port"
